"use client";

export function WorkspaceMode() {
  return (
    <section
      className="workspace-mode"
      aria-label="Full workspace mode"
      aria-hidden="true"
      data-workspace-region="workspace"
      data-state="inactive"
    />
  );
}
